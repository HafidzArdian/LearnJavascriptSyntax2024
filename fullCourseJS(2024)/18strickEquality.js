//  = Assignment Operator
//  == Comparison operator (Compare if Values Are Same)\
//  === Comparison operator (Compare if Values And Data Types Are Same)
//  != Comparison operator (Compare if Values Are Not Same)
//  !== Comparison operator (Compare if Values And Data Types Are Not Same)

const PI = 3.14;
if (PI !== "3.14") {
    console.log("That is not PI");
}
else {
    console.log("That is PI");
}
